#json 

```javascript
w
```

### JSON Functions
* `JSON.stringify` & `JSON.parse` to convert to and from JSON
	* `JSON.stringify` - converts a JavaScript object to a JSON string
	* `JSON.parse` - converts a JSON string to a Javascript object