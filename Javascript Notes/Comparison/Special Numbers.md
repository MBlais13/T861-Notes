
These values are considered numbers but do not behave the same as normal numbers
`infinity` & `-infinity` generate an infinite number in the positive and negative range.

`NaN` stands for "not a number", trying to calculate `0 / 0`, `infinity - infiniti`, etc..
